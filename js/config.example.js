/**
 * EmailJS 설정 파일 템플릿
 * 
 * 이 파일을 복사하여 config.js를 만들고 실제 키 값을 입력하세요.
 * 
 * 키 찾는 방법:
 * - Public Key: EmailJS 대시보드 → Integration → API Keys
 * - Service ID: EmailJS 대시보드 → Email Services
 * - Template ID: EmailJS 대시보드 → Email Templates
 */

const EMAILJS_CONFIG = {
    publicKey: 'YOUR_PUBLIC_KEY',      // Public Key 입력
    serviceId: 'YOUR_SERVICE_ID',      // Service ID 입력
    templateId: 'YOUR_TEMPLATE_ID'     // Template ID 입력
};

/**
 * 회사 연락처 정보
 */
const COMPANY_CONTACT = {
    phone: '02-1234-5678',              // 전화번호
    fax: '02-1234-5679',                // 팩스번호
    email: 'info@daehyung.co.kr',       // 이메일
    address: '서울특별시 강남구 테헤란로 123, 대형빌딩 5층'  // 주소
};
